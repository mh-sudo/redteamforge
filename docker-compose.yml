services:
  redteamforge:
    build: .
    image: redteamforge:local
    ports:
      - "8080:8080"
    environment:
      HOST: 0.0.0.0
      PORT: "8080"
      # AUTH_PASSWORD: "change-me"
    restart: unless-stopped
